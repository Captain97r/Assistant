<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="App5.AdminMenu">
    <Grid ColumnSpacing="-3" RowSpacing="-3" >

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>

        <Frame Grid.Column="0" Grid.Row="0">
        </Frame>

        <Frame Grid.Column="0" Grid.Row="1">
            <Button x:Name="button_join_game" Text="Присоединение к игре" Clicked="join_clicked"/>
        </Frame>

        <Frame Grid.Column="0" Grid.Row="2">
            <Button x:Name="button_create_room" Text="Создать новую комнату" Clicked="create_room_clicked"/>
        </Frame>

        <Frame Grid.Column="0" Grid.Row="3">
        </Frame>

        <Frame Grid.Column="0" Grid.Row="3">
            
        </Frame>

        <Frame Grid.Column="0" Grid.Row="5">
            <Button x:Name="logout_button" Text="Log out" Clicked="logout_clicked"/>
        </Frame>

    </Grid>

</ContentPage>